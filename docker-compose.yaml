version: '3'

services:
  simple-ai-core:
    build:
      context: .
      dockerfile: Dockerfile
    image: simple-ai-core
    container_name: simple_ai_core
    restart: always
    volumes:
      - .:/app
    ports:
      - "8083:8083"
