version: '3'

services:
  simple-ai-core:
    build:
      context: .
      dockerfile: Dockerfile
    image: simple-ai-node
    container_name: ${NODE}
    restart: always
    volumes:
      - .:/app
    ports:
      - "${PORT}:${PORT}"
